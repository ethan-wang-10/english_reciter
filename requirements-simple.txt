# 简化的Web应用依赖
# 避免需要编译的包，使用纯Python实现

# Web框架
Flask==3.0.0
Werkzeug==3.0.1

# CORS支持
Flask-CORS==4.0.0

# 生产环境 WSGI 服务器
gunicorn==21.2.0

# HTTPS（DeepSeek 等）：避免 macOS 等环境下 urllib 证书链缺失
certifi>=2024.8.30

# config.json 内 DeepSeek API Key 加密（Fernet）
cryptography>=42.0.0

# 核心功能依赖（与CLI版相同）
prettytable==3.10.0
readchar==4.0.6
nltk==3.8.1

# scripts/validate_words_csv.py --spellcheck：英语拼写检查（词频词典）
pyspellchecker>=0.8.0

# 词库：英文词形还原。模型 en_core_web_sm 走 GitHub 下载，在国内/离线服务器上容易失败；
# 部署时不强制安装模型，应用会降级为启发式词形处理。需要 VIP 课文 spaCy 分词时再手动安装模型。
spacy==3.8.13

# 头像：压缩与按需缩放（WebP）；词汇导入默认使用 RapidOCR + ONNX Runtime
Pillow==12.0.0
rapidocr==3.9.2
onnxruntime>=1.20,<2
# OCR 降级引擎；还需系统安装 Tesseract 与 eng 语言包
pytesseract>=0.3.10
