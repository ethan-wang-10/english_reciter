# 简化的Web应用依赖
# 避免需要编译的包，使用纯Python实现

# Web框架
Flask==3.0.0
Werkzeug==3.0.1

# CORS支持
Flask-CORS==4.0.0

# 生产环境 WSGI 服务器
gunicorn==21.2.0

# 核心功能依赖（与CLI版相同）
prettytable==3.10.0
readchar==4.0.6
nltk==3.8.1

# 词库：英文词形还原（需 `python -m spacy download en_core_web_sm`）
spacy==3.7.4

# 头像：压缩与按需缩放（WebP）
Pillow==10.2.0
